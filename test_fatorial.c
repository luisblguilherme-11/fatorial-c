#include <stdio.h>
#include <assert.h>
int main()
{
    assert(fatoral(0)==1);
    assert(fatoral(1)==1);
    assert(fatoral(6)==720);
    
    printf("Testes executados com sucesso!")
    printf("Criado por Luís Guilherme\n");
    return 0;
}
